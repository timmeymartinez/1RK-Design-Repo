import React from 'react';
/**
 * Underlined section heading, the way the real SOP/briefing docs mark sections (not size alone).
 * @startingPoint section="Layout" subtitle="Underlined doc section heading" viewport="500x80"
 */
export interface SectionHeadingProps {
  children: React.ReactNode;
  dark?: boolean;
}
