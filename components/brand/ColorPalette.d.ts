import React from 'react';
/**
 * The five canonical brand colors — Primary Green, Secondary Green, Brand Accent Gold,
 * Highlight Red, Off-White — with hex, token name, and usage note. Nothing else is ever introduced.
 * @startingPoint section="Brand" subtitle="The five canonical brand colors" viewport="900x260"
 */
export interface ColorPaletteProps {
  /** Height of the color swatch strip in px */
  swatchHeight?: number;
}
export declare function ColorPalette(props: ColorPaletteProps): JSX.Element;
