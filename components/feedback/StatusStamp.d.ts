import React from 'react';
/**
 * Square rubber-stamp classification mark for document headers/covers (no rotation; decorative, not a real security control).
 * @startingPoint section="Feedback" subtitle="Rubber-stamp classification mark" viewport="400x100"
 */
export interface StatusStampProps {
  label?: string;
  tone?: 'danger' | 'green' | 'gold';
}
