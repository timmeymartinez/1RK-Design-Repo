import React from 'react';
/**
 * Letterhead-style horizontal rule, optionally with a centered uppercase label.
 * @startingPoint section="Core" subtitle="Section rule with optional label" viewport="700x100"
 */
export interface DividerProps {
  label?: string;
  dark?: boolean;
}
