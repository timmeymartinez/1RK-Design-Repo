Thick letterhead-style rule, the way real briefing docs separate header from body.

```jsx
<Divider label="Rules of Engagement" />
```

Omit `label` for a plain rule. `dark` swaps to the translucent on-dark border color.
